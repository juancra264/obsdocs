to check the time zone of a linux server:

```
sudo timedatectl show --va -p Timezone
```

API to get your time zone:

```
curl http://ip-api.com/line?fields=timezone
```

or 

```
curl https://ipapi.co/timezone
```